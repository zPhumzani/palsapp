- if user_signed_in?
  .jumbotron.text-center
    %h1= "Hello #{current_user.name}"
    %p Share your inspiration and see what's inspiring others.
-else
  .jumbotron.text-center
    %h1= "What Are You Upto"
    %p Share Your Activities And See Others Activities
    = link_to "Sign In", new_user_session_path, class: "btn btn-lg btn-primary"